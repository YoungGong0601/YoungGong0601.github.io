// document.addEventListener("DOMContentLoaded", () => {
//     // .hover-scale 클래스를 가진 모든 요소를 찾음
//     const scaleItems = document.querySelectorAll(".hover-scale");

//     scaleItems.forEach((item) => {
//         // HTML에 적힌 data-scale 값을 가져옴
//         const scaleValue = item.dataset.scale;

//         // 값이 있으면 CSS 변수로 등록해줌
//         if (scaleValue) {
//             item.style.setProperty("--scale-amt", scaleValue);
//         }
//     });
// });
