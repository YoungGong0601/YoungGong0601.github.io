---
title: "MDX 동작 확인"
description: "MDX 안에서 컴포넌트와 표현식이 동작하는지 확인한다."
pubDatetime: 2026-09-22T10:40:00+09:00
tags:
  - 테스트
---

import Datetime from "@/components/Datetime.astro";

export const build = new Date("2026-09-22");

일반 마크다운 문법은 그대로 동작한다. **굵게**, `코드`, [링크](https://docs.astro.build).

표현식도 된다. 2 + 3 = {2 + 3}

아래는 사이트의 Astro 컴포넌트를 MDX 안에서 직접 쓴 것이다.

<Datetime pubDatetime={build} size="lg" />

```ts
const ok: boolean = true;
```
